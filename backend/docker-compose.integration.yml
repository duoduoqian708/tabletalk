# 集成测试数据库（PG/MySQL 适配器验证）
# 用法：
#   docker compose -f docker-compose.integration.yml up -d
#   TABLETALK_INTEGRATION=1 .venv/bin/python -m pytest tests/integration -v
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tabletalk
      POSTGRES_PASSWORD: tabletalk
      POSTGRES_DB: tabletalk_test
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tabletalk -d tabletalk_test"]
      interval: 2s
      timeout: 3s
      retries: 30

  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: tabletalk
      MYSQL_PASSWORD: tabletalk
      MYSQL_DATABASE: tabletalk_test
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -utabletalk -ptabletalk --silent"]
      interval: 2s
      timeout: 3s
      retries: 40
