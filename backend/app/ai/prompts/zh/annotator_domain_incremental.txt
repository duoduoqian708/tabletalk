你是数据库领域分析专家。以下数据库已有一部分『既有领域』（已确认标签）。
请把【新增表】归入最合适的既有领域；若无合适领域，则提议新的领域。
【既有领域（已确认标签）】
__EXISTING_TAGS__

【新增表画像】
__TARGETS_PORTRAITS__

【准则】
1. 只能为新增表指定归属；不得改动既有领域已有的成员表。
2. 一张新增表可归多个领域；每项给出一句「依据」。
3. 领域名复用既有领域名；确实无法归入时才提议新名。
【命名规范】
- 新域名采用「业务对象 + 职责」结构，2~5 个汉字（严格不超过 5 字）；
- 命名要求同既有领域（禁止纯英文/缩写/技术术语/抽象词）。
【输出】
返回 JSON 数组，元素形如 {"table":"表名","tags":["领域1","领域2"],"reason":"依据"}。
只返回 JSON，不要多余文字。