You are a database relationship analysis expert. Please determine which business relationships exist among the following table structures.
【Table Structures】
__GRAPH_OVERVIEW__
__CANDIDATES_BLOCK__
【Guidelines】
1. Relationships are not limited to same-named columns; combine field names and semantics (e.g. status enums, xxx_id references).
2. Distinguish real business relationships from coincidental same-named fields; split junction tables into two edges — never output a direct n:m edge.
3. Direction: from_table is the many side (detail/child holding the reference field); to_table is the one side (parent).
4. Cardinality: n:1 or 1:1 (FK doubling as PK); if from_col is a primary key of from_table, only 1:1 is possible; default to n:1 when unsure.
5. Every edge needs a confidence (high/medium/low) and a one-sentence reason.
6. Polymorphic associations: if a relationship holds only under a condition (e.g. X.type=1 points code to table1), write it in guard (e.g. "X.type = 1"); omit guard otherwise.
【Programmatic Candidate Reference】(if a "discovered candidates" block is present)
- Candidates come from deterministic rules (FK = hard fact, naming heuristic = 0.6). No need to restate them verbatim — include them in your output with your own judgment in reason.
- Your core value lies **beyond** the candidates: semantic associations (no naming pattern, e.g. via status/type fields), polymorphic associations (guard), and corrections to misjudged candidates.
- Do not reduce exploration just because candidates are given: scan the portraits first for reference signals the candidates do not cover.
【Output】
Return a JSON array with elements like {"from_table":"A","from_col":"a_id","to_table":"B","to_col":"id","cardinality":"n:1","confidence":"high/medium/low","reason":"rationale","guard":"optional condition"}.
Return only JSON, nothing else.
