# DATUM V2 Implementation Plan（总计划 P1–P6）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-08-19-datum-v2-vision-design.md` 落地 V2：图谱画布工作区 + Void 视觉 + 三模式图库 + 表格视图 + AI 交互重做 + 技能可编排，V1 功能零删除。

**Architecture:** 前端 React SPA（zustand，自研轻量 SVG 画布引擎），后端 FastAPI sidecar（tools/skills 注册表演进为剧本执行器 + 技能广场 API）。每个子项目独立提交、独立验证（typecheck + build + pytest + 重启 sidecar + 手测）。

**Tech Stack:** React 18 + TS + Vite · zustand · 原生 SVG（无图库依赖）· FastAPI · pytest

**完成门槛（每子项目）**：`cd frontend && npm run typecheck && npm run build` 全绿；`cd backend && .venv/bin/python -m pytest -q` 全绿（P1-P5 后端无改动时跑基线确认）；重启 sidecar（`lsof -ti tcp:8765 | xargs kill -9` 后重启）；浏览器手测关键路径。

---

## P1 · 设计系统与视觉重构（Void 深空极简）

**目标**：全站换 Void 视觉 + 令牌化；布局骨架升级（顶栏/主区/AI停靠栏）；功能与交互不变。

**Files:**
- Modify: `frontend/src/renderer/src/styles/tokens.css`（全量重写：Void 令牌 + 旧变量名映射）
- Modify: `frontend/src/renderer/src/styles/app.css`（令牌化 + 组件新皮肤）
- Modify: `frontend/src/renderer/src/styles/boot.css`（启动屏新皮肤）
- Modify: `frontend/src/renderer/src/styles/review.css`（知识审查新皮肤）
- Modify: `frontend/src/renderer/src/components/AppLayout.tsx`（顶栏重构：连接/搜索/gate/模型/设置/审计）
- Modify: `frontend/src/renderer/src/components/AiRail.tsx`（头部 provider/模型展示收敛；皮肤类名）
- Modify: `frontend/src/renderer/src/components/SchemaRail.tsx`、`DataTable.tsx`、`SettingsDrawer.tsx`、`ConnectionModal.tsx`、`ConnectionMenu.tsx`、`ModulePages.tsx`、`KnowledgeReview.tsx`、`KnowledgeGraph.tsx`、`ReportCard.tsx`（仅必要的类名/结构微调以配合新皮肤）
- Test: 手测 + `npm run screenshot`（Playwright 截图）

- [ ] **Step 1: 重写 tokens.css（完整 Void 令牌）**

```css
:root {
  /* ── Void 深色调色板 ── */
  --void-0: #0a0b0e;  --void-1: #0d0e12;  --void-2: #111318;
  --void-3: #14161d;  --void-4: #1c1f27;  --void-5: #232936;
  --ink: #e8eaf0;  --ink-dim: #8b93a3;  --ink-faint: #5b6472;
  --line: rgba(255,255,255,0.07);  --line-strong: rgba(255,255,255,0.14);
  --accent: #7c8cff;  --accent-dim: rgba(124,140,255,0.14);
  --accent-soft: #aeb6ff;  --accent-ink: #0a0b0e;
  --green: #6ee7b7;  --green-dim: rgba(110,231,183,0.12);
  --amber: #f6ad55;  --amber-dim: rgba(246,173,85,0.12);
  --red: #f87171;  --red-dim: rgba(248,113,113,0.12);
  --blue: #7c8cff;  --blue-dim: rgba(124,140,255,0.14);
  /* ── 旧变量名映射（V1 兼容别名，逐步替换后删除） ── */
  --bg: var(--void-0);  --bg-2: var(--void-1);  --panel: var(--void-1);
  --card: var(--void-3);  --card-hi: var(--void-4);  --rail: var(--void-1);
  --ink-dim: var(--ink-dim);  /* 见上 */
  --line: var(--line);  --line-strong: var(--line-strong);
  --teal: var(--accent);  --teal-dim: var(--accent-dim);
  --amber: var(--amber);  --amber-dim: var(--amber-dim);
  --red: var(--red);  --red-dim: var(--red-dim);
  --green: var(--green);  --green-dim: var(--green-dim);
  --blue: var(--blue);  --blue-dim: var(--blue-dim);
  /* ── 几何 ── */
  --r-sm: 6px;  --r-md: 8px;  --r-lg: 12px;  --r-full: 99px;
  --shadow-1: 0 2px 12px rgba(0,0,0,0.35);
  --shadow-2: 0 8px 28px rgba(0,0,0,0.55);
  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;  --sp-4: 16px;  --sp-5: 24px;
  --topbar-h: 42px;  --rail-w: 36px;  --ai-w: 340px;
  --z-sticky: 30;  --z-pop: 60;  --z-modal: 90;  --z-drawer: 100;
  --ease: cubic-bezier(0.2, 0.8, 0.24, 1);  --dur: 160ms;
  /* ── 域色（图谱） ── */
  --domain-1: #7c8cff;  --domain-2: #6ee7b7;  --domain-3: #f6ad55;
  --font-sans: 'Hanken Grotesk', 'PingFang SC', 'Microsoft YaHei', -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', 'SFMono-Regular', Menlo, Consolas, monospace;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
html, body, #root { height: 100%; }
body {
  font-family: var(--font-sans);
  background:
    radial-gradient(1100px 500px at 88% -8%, rgba(124,140,255,0.05), transparent 60%),
    radial-gradient(900px 460px at -6% 108%, rgba(124,140,255,0.03), transparent 55%),
    var(--void-0);
  color: var(--ink);
  -webkit-font-smoothing: antialiased;
  overflow: hidden;
  user-select: none;
}
::selection { background: rgba(124,140,255,0.25); }
.mono { font-family: var(--font-mono); }
button, input, textarea { font-family: inherit; }
/* 滚动条 */
::-webkit-scrollbar { width: 10px; height: 10px; }
::-webkit-scrollbar-thumb { background: var(--void-5); border-radius: 99px; border: 2px solid transparent; background-clip: content-box; }
::-webkit-scrollbar-thumb:hover { background-color: #3a4150; background-clip: content-box; }
::-webkit-scrollbar-track { background: transparent; }
```

- [ ] **Step 2: app.css 令牌化（机械替换 + 关键组件精修）**

策略：`grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' app.css` 列出硬编码颜色 → 逐个映射到令牌（背景→void、边框→line、文字→ink、强调→accent/domain）；**精修清单**（类名 → 要求）：
- `.appbar`：`background: var(--void-1)`、`border-bottom: 1px solid var(--line)`、`height: var(--topbar-h)`；logo 方块 `background: var(--accent)` 圆角 `var(--r-sm)`；`.cur-table` 用 `--accent-soft`。
- `.nav .tab`：hover `--void-4`；`.on`：`color: var(--ink)` + 下边框/底色 `var(--accent-dim)`。
- `.gw`、`.sys-btn`：文字 `--ink-dim`；hover `--void-4`。
- `.schemaview`：`background: var(--void-1)`、`border-right: 1px solid var(--line)`；节点/字段 hover `--void-4`；选中行 `--accent-dim`。
- `.airail`：`background: var(--void-1)`、`border-left: 1px solid var(--line)`；气泡/卡片 `--void-3`、边框 `--line-strong`；输入框 `--void-3` 聚焦描边 `var(--accent)`。
- `.datatable` 表头 `color: var(--ink-faint)`、行 hover `--void-4`、选中行 `--accent-dim`；`td` 边框 `var(--line)`。
- `.set-drawer`：`background: var(--void-1)`、`box-shadow: var(--shadow-2)`；`--set-mask` `rgba(0,0,0,0.55)`。
- `.onboarding .card`：`background: var(--void-3)`、`border: 1px solid var(--line-strong)`、`box-shadow: var(--shadow-2)`。
- `.primary`：`background: var(--accent)`、`color: var(--accent-ink)`、`border-radius: var(--r-md)`、hover 提亮 `#8f9dff`。
- `.iconbtn`、`.mini-btn`、`.btn`、`.sys-btn`：`--void-4` hover、`--r-md`。
- 徽章色（allow/executed 绿、review 琥珀、block/red 红）统一走 `--green/--amber/--red` + dim 底。
- 字体：所有 `font-family` 收敛 `var(--font-sans)` / `var(--font-mono)`。

- [ ] **Step 3: boot.css 新皮肤**：`.boot` 背景 `--void-0`；`.boot-card` `--void-3` + `--line-strong` 边框 + `--shadow-2`；wordmark `.dot` `--accent`；spinner 色 `--accent`；statusline 文字 `--ink-faint`。

- [ ] **Step 4: review.css 新皮肤**：卡片/行/按钮同上令牌映射；判定徽章走语义色。

- [ ] **Step 5: AppLayout 顶栏重构（骨架升级）**

```tsx
// 顶栏结构（功能不变：tab 切换、连接菜单、设置、审计入口保留）
<header className="appbar">
  <span className="wordmark"><span className="dot" />DATUM <small>DB</small></span>
  <ConnectionMenu onNew={() => setModalOpen(true)} />
  <nav className="nav">{TABS.map(...)}</nav>
  <span className="cur-table mono" title="当前展示">{subject}</span>
  <span className="spacer" />
  <div className="gw mono">
    <span className="gw-model">{health?.ai_provider_name ?? '—'}</span>
    <span className="gw-divider" />
    <span>{health?.ai_model ?? '—'}</span>
    {health?.ai_mock_downgraded && <span className="gw-warn">· mock降级</span>}
  </div>
  <button className="sys-btn" onClick={() => setSettingsOpen(true)}><span className="gear">⚙</span></button>
</header>
```

- [ ] **Step 6: 组件微调**（SchemaRail/AiRail/DataTable/SettingsDrawer/ConnectionModal/ModulePages/KnowledgeReview/ReportCard）：仅改类名或补 class 以配合 Step 2 新皮肤；行为零改动。

- [ ] **Step 7: 验证**
  - `cd frontend && npm run typecheck` → 0 error
  - `cd frontend && npm run build` → dist 更新
  - `cd backend && .venv/bin/python -m pytest -q` → 基线 184 passed（后端未动）
  - 浏览器手测：顶栏各元素、tab 切换、连接菜单、设置抽屉、AI 对话、表格
  - `npm run screenshot`（后台，产出 docs/screenshots/knowledge-review.png）

- [ ] **Step 8: Commit** `feat: P1 Void 设计系统——全站令牌化+顶栏骨架+组件新皮肤`

---

## P2 · 图谱画布引擎

**目标**：主区默认视图变为图谱画布；节点/边/域色/注释状态/查询节点；缩放平移；浏览/搭查/治理三模式可切换；单击=检查器、双击=打开数据、右键=更多。

**Files:**
- Create: `frontend/src/renderer/src/store/graph.ts`（useGraph：nodes/edges/viewport/selected/mode/tags）
- Create: `frontend/src/renderer/src/components/GraphCanvas.tsx`（SVG 渲染 + 视口交互 + 模式切换）
- Create: `frontend/src/renderer/src/components/NodeInspector.tsx`（检查器浮层）
- Modify: `frontend/src/renderer/src/api/schema.ts`、`api/knowledge.ts`（图谱数据聚合：schema + FK + 已确认标签/注释）
- Modify: `frontend/src/renderer/src/components/AppLayout.tsx`（主区默认 GraphCanvas；SchemaRail 退役为可折叠列表兜底）

**Tasks:** 数据聚合（节点=表+域色+注释状态；边=FK；查询节点=v1 无则先留空）→ 渲染（SVG 网格底/节点卡片/曲线边/标签）→ 视口（拖拽平移/滚轮缩放/⤢ 全览）→ 布局（域分组网格兜底 + 力导向增强）→ 模式状态机（浏览/搭查/治理）→ 交互（单击检查器、双击表格视图、右键菜单）→ 验证 + Commit。

**完成定义**：连接 demo 库后主区即图谱；三模式切换可用；双击节点跳表格视图（P3 先留占位跳转可用）；检查器显示字段/注释/标签。

---

## P3 · 表格视图 + 左缘微条 + 多标签结果

**目标**：⌘1/⌘2 切换图谱⇄表格；表格全屏（筛选/排序/分页/列设置）；左缘 36px 微条（回图谱 + 关联表跳转）；结果多标签（数据浏览/AI查询/报告）。

**Files:**
- Create: `frontend/src/renderer/src/components/TablePane.tsx`（表格视图 + 工具栏 + 微条）
- Modify: `frontend/src/renderer/src/store/results.ts`（result/report → tabs 数组）
- Modify: `frontend/src/renderer/src/components/DataTable.tsx`（分页/列设置/单元格复制增强）
- Modify: `frontend/src/renderer/src/api/query.ts`（分页 offset/limit 已支持则直连）
- Modify: `frontend/src/renderer/src/components/AppLayout.tsx`（视图切换 + 快捷键）

**完成定义**：双击节点进表格；⌘1/⌘2 秒切；微条关联表跳转；AI 结果与数据浏览多标签并存；安全不变式回归（只读自动执行等）。

---

## P4 · AI 交互区重做

**目标**：去步骤条/徽章堆砌；思考折叠一行；上下文筹码（随选中联动 + @表名）；SQL 卡片就地编辑重跑；主动建议条；结果进主区表格。

**Files:**
- Modify: `frontend/src/renderer/src/components/AiRail.tsx`（重构对话流渲染 + 上下文筹码 + 建议条 + SQL 可编辑）
- Modify: `frontend/src/renderer/src/store/chat.ts`（上下文/建议状态）
- Modify: `frontend/src/renderer/src/api/ai.ts`（如有需要的事件字段）
- Modify: `frontend/src/renderer/src/components/AppLayout.tsx`（停靠栏宽度/折叠）

**完成定义**：四步阶段条移除；思考一行可展开；选中节点自动入上下文；@表名 生效；SQL 卡片可编辑重跑；建议条一键采纳；闸门/审计回归全绿。

---

## P5 · 知识治理图上化 + 检查器编辑

**目标**：检查器显示/编辑注释与标签；治理模式在图上确认/驳回；健康度统计。

**Files:**
- Modify: `frontend/src/renderer/src/components/NodeInspector.tsx`（注释/标签编辑）
- Create: `frontend/src/renderer/src/components/GovernancePanel.tsx`（治理模式统计 + 确认/驳回流）
- Modify: `backend/app/api/knowledge.py`（按表取注释/标签端点、批量确认，若缺）
- Test: `backend/tests/api/test_knowledge.py`（新增端点用例）

**完成定义**：检查器可编辑注释/标签并落库；治理模式确认/驳回即改节点状态；后端 pytest 全绿。

---

## P6 · 工具×技能解耦 + 技能广场

**目标**：技能=声明式剧本（工具组合+提示词+触发词）；技能广场（启用/禁用/组合/自建）；组合安全校验。

**Files:**
- Modify: `backend/app/ai/skills/skill.py`、`registry.py`（剧本模型：tools[]/prompt/triggers + 校验）
- Modify: `backend/app/ai/agent/dispatcher.py`（剧本执行器分支）
- Create: `backend/app/api/skills.py`（GET/PUT/POST /api/v1/skills）
- Modify: `backend/app/main.py`（挂载路由）
- Test: `backend/tests/ai/test_skills.py`、`backend/tests/api/test_skills_api.py`（新增）
- Create: `frontend/src/renderer/src/components/SkillPlaza.tsx`（设置内技能广场）
- Modify: `frontend/src/renderer/src/components/SettingsDrawer.tsx`（新 tab）
- Modify: `frontend/src/renderer/src/api/skills.ts`

**完成定义**：内置 query/report 剧本化且行为不变；技能广场可启用/禁用/勾选工具/自建；自建技能端到端可用；安全校验（AI 工具集无 DDL 执行）用例覆盖；后端 pytest 全绿（基线 184 + 新增）。

---

## 验证与收尾（每子项目后）

- 前端 `npm run typecheck && npm run build`
- 后端 `.venv/bin/python -m pytest -q`（改动相关时全量）
- 重启 sidecar + `curl /api/v1/health` 确认
- 浏览器手测关键路径；`npm run screenshot` 定期跑
- 每子项目独立 commit，信息带 `P{n}` 前缀
