# 待办

> 口述记录：说到哪记到哪，原话整理。

- [ ] 意图识别：去掉第1步关键词匹配，意图分类全部走大模型；取消 mock/本地大模型概念，只有用户自配的大模型可用。
- [ ] 意图识别-上下文检查：意图识别时结合该 session 的历史对话 + 当前对话内容做上下文检查；查数据/改数据若参数缺失则先澄清再执行（先问再查，不给默认值）。
- [ ] 前端展示-动态子任务进度（必改）：查数据等技能命中后，前端展示=总任务进度（意图分类结果）+ 动态子任务进度；子任务维度=后端实际调用的 tool（非写死4步），后端调哪个前端就展示哪个，子任务为动态加载，且每一步需做前后端流式输出内容。
- [x] 归档：意图/技能/工具 定义 v1 已落 `docs/意图-技能-工具定义-v1.md`（6 意图 / 10 工具 / 场景矩阵，审核圈为语义审核 tool，硬门禁不暴露）
- [ ] 打磨：逐把工具/逐个场景的参数与实现细化（待下一轮）
